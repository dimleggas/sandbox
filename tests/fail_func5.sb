
(bit a, bit b, bit cin) fulladder (bit s, bit c):
	a ^ b -> bit z
	z -> s
	cin -> c

(bit a, bit b, bit cin) fulladder (bit s, bit c):
	a ^ b ^ cin -> s
	(a & b) ^ (cin & (a ^ b)) -> c

(bit.4 a, bit.4 b, bit.3 cin) sandbox (bit.4 s, bit.4 c):
	[a, b, cin] fulladder [s, c]